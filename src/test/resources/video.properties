video.folder=target/videos
video.enabled=true
video.save.mode=ALL
recorder.type=FFMPEG

# video.save.mode can be:
# ALL ? save all videos
# FAILED_ONLY ? only when a failure occurs
# ANNOTATED ? only if there is an @Video annotation
